
* Implement gate permissions when Permissions plugin supports adding a gate to a group.
* Implement new gate design: Jail
* add back call to gate's to do lightning/effects

Testing:
    * new teleportation shit
    * banned player detection when teleporting
    * behavior of connection state when, e.g., a player is banned on destination server
    * server-server chat format
    * server-server send/receiveAllChat options

Wiki Updates:
    * update GateDesign with lightning
    * update server add commands with renamed args
    * remove server change command
    * update ServerToServer:
        * heading note about experimental NAT support
        * scenario 1 new "trp server list" output format
        * section about network and server options
    * global configuration:
        * sendPrivateAddress (default true)
        * usePrivateAddress (default true)
        * useBOSEconomy (default false)
        * deleteDebugFile (default true)
        * clusterName (default null)
        * usePermissionsEx (default false)
        * gateLockExpiration (default 2000ms)
        * arrivalWindow (default 20000ms)
    * home page
        * BOSEconomy support
    * gate design: iConomy -> Economy