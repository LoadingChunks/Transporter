
* Implement gate permissions when Permissions plugin supports adding a gate to a group.
* Implement new gate design: Jail
* fix minecarts - too fast same world, no velocity on exit between servers

Testing:

Wiki Updates:
    * remove save servers/reload servers commands
    * remove server set/get craftProxy commands
    * remove server set/get minecraft command
    * document httpProxy.[type|host|port|user|password] settings
    * document debugURL setting
    * document servers.[pluginAddress|enabled|key|name] settings
    * update ClientPatcher to reflect support for BukkitContrib
    * add Dynmap page


